import { render, screen } from '@testing-library/react';
import App from './App';

describe("Render app", () => {
  test('renders react customer front end app', () => {
    // render(<App />);
    // linkElement = screen.getByTestId('appcomponent');
    // expect(linkElement).toBeInTheDocument();
    expect(1+2).toBe(3)
  });
})

